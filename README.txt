/* Database Configurations */
    Database = publication
    username = root
    password = root


/* Spring boot application run */
    mvn spring-boot:run


/* Clone project */
    https://github.com/PasinduD95/Book-Publication.git



/* Assumptions */
User wants to like a book. So create User table.
There are many categories. So create table for category.
Doesn't save a book, a user, a category and an author if exist in the db.